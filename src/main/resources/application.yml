spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/Callback-UAZ"
    username: "postgres"
    password: "qwerty"
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    generate-ddl: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
    database: postgresql
    database-platform: "org.hibernate.dialect.PostgreSQL10Dialect"
server:
  port: 8070